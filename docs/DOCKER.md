# 🐳 Запуск проекта в Docker

Это руководство поможет вам запустить Code Explainer в Docker на Windows Desktop.

## Предварительные требования

1. **Docker Desktop для Windows** - скачайте и установите с [официального сайта](https://www.docker.com/products/docker-desktop/)
2. Убедитесь, что Docker Desktop запущен и работает

## Быстрый старт

### 1. Сборка и запуск через Docker Compose (рекомендуется)

```bash
# Соберите и запустите контейнер
docker-compose up --build

# Или запустите в фоновом режиме
docker-compose up -d --build
```

После успешного запуска приложение будет доступно по адресам:
- **Основной интерфейс**: http://localhost:8000/static/index.html
- **Страница истории**: http://localhost:8000/static/history.html
- **Документация API**: http://localhost:8000/docs
- **Health check**: http://localhost:8000/health

### 2. Остановка контейнера

```bash
# Остановка с сохранением данных
docker-compose down

# Остановка с удалением volumes (удалит базу данных!)
docker-compose down -v
```

### 3. Просмотр логов

```bash
# Просмотр логов в реальном времени
docker-compose logs -f

# Просмотр последних 100 строк логов
docker-compose logs --tail=100
```

## Запуск без Docker Compose

Если вы предпочитаете использовать только Docker:

```bash
# Сборка образа
docker build -t code-explainer:latest .

# Запуск контейнера
docker run -d \
  --name code-explainer-app \
  -p 8000:8000 \
  -v ./backend/data:/app/backend/data \
  -e USE_MOCK_LLM=true \
  -e DATABASE_DIR=/app/backend/data \
  code-explainer:latest
```

## Структура Docker-конфигурации

### Dockerfile
- Использует Python 3.11-slim образ
- Устанавливает зависимости из `requirements.txt`
- Копирует код приложения
- Открывает порт 8000
- Запускает FastAPI через uvicorn

### docker-compose.yml
- Настраивает сервис `code-explainer`
- Маппит порт 8000
- Создает volume для базы данных в `./backend/data`
- Настраивает переменные окружения
- Включает health check

### .dockerignore
- Исключает ненужные файлы из образа (кэш, логи, .git и т.д.)

## Персистентность данных

База данных SQLite сохраняется в директории `./backend/data` на вашем хосте через Docker volume. Это означает:
- ✅ Данные сохраняются между перезапусками контейнера
- ✅ Данные доступны даже после удаления контейнера
- ✅ Вы можете делать резервные копии, копируя файл `backend/data/code_explainer.db`

## Переменные окружения

Вы можете настроить приложение через переменные окружения в `docker-compose.yml`:

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `USE_MOCK_LLM` | Использовать мок-сервис вместо реального LLM API | `true` |
| `DATABASE_DIR` | Директория для базы данных в контейнере | `/app/backend/data` |

### Пример: использование реального LLM API

Отредактируйте `docker-compose.yml`:

```yaml
environment:
  - USE_MOCK_LLM=false
  - HUGGINGFACE_API_KEY=your_api_key_here
  - DATABASE_DIR=/app/backend/data
```

Затем пересоберите контейнер:

```bash
docker-compose up --build -d
```

## Разработка с hot-reload

Для разработки с автоматической перезагрузкой при изменении кода:

1. Раскомментируйте volumes в `docker-compose.yml`:
```yaml
volumes:
  - ./backend/data:/app/backend/data
  - ./backend:/app/backend
  - ./frontend:/app/frontend
```

2. Перезапустите контейнер:
```bash
docker-compose down
docker-compose up
```

⚠️ **Примечание**: Для hot-reload также нужно изменить команду в Dockerfile на `uvicorn backend.app:app --host 0.0.0.0 --port 8000 --reload`

## Устранение неполадок

### Контейнер не запускается

1. Проверьте, что порт 8000 свободен:
```bash
netstat -ano | findstr :8000
```

2. Посмотрите логи:
```bash
docker-compose logs code-explainer
```

3. Проверьте, что Docker Desktop запущен

### Проблемы с правами доступа к базе данных

На Windows обычно проблем нет, но если возникает ошибка:
```bash
# Убедитесь, что директория существует
mkdir -p backend/data
```

### Очистка и пересборка

Если возникли проблемы, попробуйте полную пересборку:

```bash
# Остановка и удаление контейнеров
docker-compose down

# Удаление образа
docker rmi code-explainer-app

# Пересборка без кэша
docker-compose build --no-cache

# Запуск
docker-compose up
```

### Проверка состояния контейнера

```bash
# Список запущенных контейнеров
docker ps

# Детальная информация о контейнере
docker inspect code-explainer-app

# Выполнение команд внутри контейнера
docker exec -it code-explainer-app bash
```

## Полезные команды

```bash
# Перезапуск контейнера
docker-compose restart

# Обновление образа и перезапуск
docker-compose up -d --build

# Просмотр использования ресурсов
docker stats code-explainer-app

# Удаление всех неиспользуемых образов и контейнеров
docker system prune -a
```

## Безопасность

⚠️ **Для продакшена** рекомендуется:
- Изменить `allow_origins` в CORS настройках (в `backend/app.py`)
- Использовать секреты Docker или файл `.env` для чувствительных данных
- Настроить HTTPS через reverse proxy (nginx/traefik)
- Ограничить ресурсы контейнера через `deploy.resources` в docker-compose.yml

## Дополнительные ресурсы

- [Docker Desktop документация](https://docs.docker.com/desktop/)
- [Docker Compose документация](https://docs.docker.com/compose/)
- [FastAPI документация](https://fastapi.tiangolo.com/)

